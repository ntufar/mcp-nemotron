{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "list_directory",
  "description": "List contents of a directory with metadata",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "Absolute path to the directory to list",
      "pattern": "^/"
    },
    "include_hidden": {
      "type": "boolean",
      "description": "Whether to include hidden files/directories",
      "default": false
    },
    "max_depth": {
      "type": "integer",
      "description": "Maximum depth for recursive listing (0 = current directory only)",
      "minimum": 0,
      "maximum": 10,
      "default": 0
    }
  },
  "required": ["path"],
  "additionalProperties": false
}